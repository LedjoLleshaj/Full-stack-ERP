services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: selita_fish_frontend
    ports:
      - "4200:4200"
    volumes:
      - /frontend/node_modules
      - .:/frontend
